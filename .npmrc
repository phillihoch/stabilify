# npm configuration for stabilify

# Provenance wird automatisch mit Trusted Publishing generiert
# provenance=true ist der Standard, muss nicht explizit gesetzt werden

# Registry (wird auch von setup-node gesetzt, aber hier zur Klarheit)
registry=https://registry.npmjs.org/

# Für scoped packages - öffentlich veröffentlichen
access=public

